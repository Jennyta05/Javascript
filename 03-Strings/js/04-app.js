const producto = '   Monitor 20    ';

//eliminar espacios en blanco
console.log ( producto.trimStart()); //elimina espacios del inicio
console.log ( producto.trimEnd()); //elimina espacios del final
//para eliminar ambos
console.log ( producto.trimStart(), trimEnd()); 